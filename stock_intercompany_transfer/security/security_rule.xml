<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="rule_stock_transfer_account_current_company" model="ir.rule">
            <field name="name">Stock Transfer Account: Current Company Only</field>
            <field name="model_id" ref="model_stock_transfer_account"/>
            <field name="domain_force">[('company_id', '=', company_id)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        </record>

    </data>
</odoo>